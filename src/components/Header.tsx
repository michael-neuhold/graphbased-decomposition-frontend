import { Heading, Pane } from "evergreen-ui"

export const Header = () => {
  return (
    <Pane padding={16}>
      <Heading size={500}>
        Graphbased decomposition
      </Heading>
    </Pane>
  )
}