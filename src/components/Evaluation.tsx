import { Heading, Pane } from "evergreen-ui"

export const Evaluation = () => {
  return (
    <Pane>
      <Heading size={500} paddingBottom={20}>
        Git Repository
      </Heading>
    </Pane>
  )
}